[package]
name = "annp-cuda"
version.workspace = true
edition.workspace = true
authors.workspace = true

[features]
default = ["cuda"]
cuda = []

[dependencies]
annp-core = { path = "../annp-core" }

[build-dependencies]
cc = "1.0"
